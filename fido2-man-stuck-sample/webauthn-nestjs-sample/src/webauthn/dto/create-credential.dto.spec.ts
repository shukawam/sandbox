import { CreateCredentialDto } from './create-credential.dto';

describe('CreateCredentialDto', () => {
  it('should be defined', () => {
    expect(new CreateCredentialDto()).toBeDefined();
  });
});
